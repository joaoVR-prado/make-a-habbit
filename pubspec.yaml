name: make_a_habbit
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  build_runner: ^2.10.5
  flutter:
    sdk: flutter
  hive_ce: ^2.18.0
  hive_ce_generator: ^1.10.0
  riverpod: ^3.1.0
  uuid: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
